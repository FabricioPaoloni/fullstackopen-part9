Exercise 9.1: The bmi calculator -COMPLETED-
Create a script for typechecking and running the program with the command npm run calculateBmi. -done

Exercise 9.2: Exercise calculator
1. Create the code of this exercise in file exerciseCalculator.ts in the same project with the previous exercise. -done
2.For the Result object, you should create an interface -done
-COMPLETED-

Exercise 9.3. Command line
Change the previous exercises so that you can give the parameters of bmiCalculator and exerciseCalculator as command-line arguments.
1- change bmiCalculator -done; had to change the script calculateBmi to make it work
2- change exerciseCalculator.ts -done now accepts some variables from the terminal, up to 8, all of them must be numbers. If the user introduce more than 8 variables, an error will be printed.
-COMPLETED-

Exercise 9.4: Express
We will continue to build the app of the previous exercises.
Add now Express to the app dependencies and create an HTTP GET endpoint hello that answers 'Hello Full Stack!'
The web app should be started with the commands npm start in production mode and npm run dev in development mode.
Replace also your existing tsconfig.json file with the following content: (web image)
1. install Express -done
2. install express types -done
3. create index.ts -> import express and build an express app with a GET http route
4. create the scripts "start" and "dev" (install concurrently library)
5. expand tsconfig.json  -done
-COMPLETED-

Exercise 9.5: WebBmi
1. req.query - configure in index.ts -done
2. check for height and weight number types (type validation) -done
3- export calculateBmi function -done
4- create GET method for webBmi -done
5- Consider adding the condition process.argv[1] === import.meta.filename to the file bmiCalculator.ts -done
-COMPLETED-

Exercise 9.6: Eslint
Configure your project to use the above ESLint settings and fix all the warnings.
1. install as dev: eslint, @eslint/js and typescript-eslint with de commando - npm install --save-dev eslint @eslint/js typescript-eslint -done
2. Write the rules to eslint.config.mjs -done
3. set up a lint npm script -done
4. install and configure @stylistic/eslint-plugin -done
5. solve all the errorss
-COMPLETED-

Exercise 9.7. WebExercises
Add an endpoint to your app for the exercise calculator. It should be used by doing a HTTP POST request to the endpoint http://localhost:3000/exercises
1.  import exerciseCalculator and create the post route -done
-COMPLETED- 

Exercise 9.8: Checkup:
1. changed some file so tests passes (error description, error status)
-COMPLETED-

Exercise 9.9. Patientor backend, step1
1. init npm project -done
2. config package.json (type:"module") -done
3. install typescript and configure tsconfig.json -done
4. install express, @types/express, ESlint  -done (OJO con typescript-eslint no me dejó instalar por fallo de compatibilidad)
5. config lint rules -done
6. install concurrently -done
7. config package.json scripts -done
8. create the initial server and a HTTP GET method for /api/ping -done
-COMPLETED-

Exercise 9.10: Patientor backend, step2
/patientor/frontend:
1. install dependencies with npm install -done
2. change backend port to 3000 as ours -done
3. install CORS in our backend with npm install cors -done
4. implement cors in the backend with import and app.use(cors())
5. install @types/cors to correct the error showed by VSCode - npm i --save-dev @types/cors -done
app working fine
-COMPLETED-

Exercise 9.11: Patientor backend, step3
1. get the files diagnoses.ts and patients.ts from the link, store it in /data -done
2. create a type Diagnosis and use it to create an endpoint /api/diagnosis for fetching all diagnoses with HTTP GET
Note that diagnoses may or may not contain the field latin. You might want to use optional properties (opens in a new tab) in the type definition.
Hint: Enabling the ESLint rule consistent-type-imports could save you from many headaches.
    2.a. create Diagnosis type in /src/types.ts -done
    2.b. create /routes/diagnosesRouter.ts -done
    2.c. use the created route in index.ts for the endpoint /api/diagnoses -done
    2.d. create src/services directory and place diagnosesService.ts with the functions to fetching and saving diagnoses -done
    2.e. implement the new service in the router -done
    2.f. change diagnoses.ts so it exports typed data -done
    2.g. add ESLint rule consistent-type-imports -done
-COMPLETED-

Ejercicio 9.12: Patientor backend, step4
Create data type Patient and set up the GET endpoint /api/patients which returns all the patients to the frontend, excluding field ssn. Use a utility type (opens in a new tab) to make sure you are selecting and returning only the wanted fields.
In this exercise, you may assume that field gender has type string.
1. create the new types in types.ts: Patient and NonSensitivePatient -done
2. edit patients.ts so it exports typed data -done
3. create patientService.ts and services -done
4. create patientsRoutes.ts -done
5. import and use patientsRoutes in index.ts -done
6. manual test http://localhost:3000/api/patients -> working fine, ssn is not shown. -done
7. manual test http://localhost:5173/ -> working fine, Patient list showing the patients -done
-COMPLETED-

Exercise 9.13: Patientor backend, step5
Create a POST endpoint /api/patients for adding patients. Ensure that you can add patients also from the frontend. You can create unique ids of type string using the uuid (opens in a new tab) library.
1. install uuid -done
2. create newPatientEntry type for typing new patients info -done
2. extends addPatient function in patientsService.ts -done
3. create the post method -done
tested with frontend. Working fine. No validations implemented yet, no gender type created :/.
-COMPLETED-

Exercise 9.14: Patientor backend, step6
Instructions
Set up safe parsing, validation and type predicate to the POST /api/patients request.
Refactor the gender field to use a const object based type.
1. create the gender field missing last exercise, created as a const object in types.ts -done
2. create the gender type based on the const -done
3. create parser function for each field in utils.ts -done
4. create validation for each field -done
5. validate the request object as object and the inclusion of the needed key values -done
6. implement the new parser function parseNewPatientEntry in patientsRoutes's post method -done
tested from the frontend, working as expected
-COMPLETED-

Exercise 9.15: Patientor backend, step 7
Use Zod to validate the requests to the POST endpoint /api/patients.
1. install Zod -done
2. import Zod -done
3. create new zod schema -done
4. base the parsing on the new zod schema -done
5. add error handling in the router -done
-COMPLETED- tested with frontend, working fine.

Exercise 9.16: Checkup
Todos los tests pasaron localmente, falta habilitar lo de github y testear en github actions
Similarly to Exercise 8, run the tests in the directory patientor-api-tests. Tests assume that the backend runs on port 3001.
1. Enable those also in GitHub by modifying .github/workflows/patientor-api-tests.yml as follows -done
2. push to github

